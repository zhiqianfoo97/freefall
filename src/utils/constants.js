import {Dimensions} from 'react-native';
const {height, width} = Dimensions.get('window');

const Constants = {TOP_PIPE_WIDTH: 250,BOTTOM_PIPE_WIDTH: 100, HEIGHT: height, WIDTH: width};
export default Constants;